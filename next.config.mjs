/** @type {import('next').NextConfig} */
const nextConfig = {
    // experimental: {
    //     typedRoutes: true,
    // },
    output: 'export',
    typedRoutes: true,
};

export default nextConfig;
